# Sections

## 1. Repository shape (repo) — HIGH — module functions over premature factories; one file per entity via getDb

## 2. Migrations (migration) — CRITICAL — numbered forward-only migrations; SQLite under db/

## 3. Tests (test) — HIGH — sibling coverage for constraints and ordering
